var pl400Questions = [
  {
    id: 1021,
    text: 'You are a Power Apps maker creating a chat bot for a website.\n\nThe chat bot must recognize geographic attributes to enable additional functionality.\n\nYou need to recommend a feature.\n\nWhat should you recommend?',
    type: 'single',
    choices: [
      'Fallback topic',
      'Power Automate Flow',
      'Bot Service compliance',
      'Slot filling',
    ],
    correct: [3],
    explanation: 'Slot filling is the correct answer because it allows the chatbot to recognize and extract specific pieces of information (like geographic attributes) from user input to populate entity slots. Fallback topic handles unrecognized intents rather than extracting data. Power Automate Flow is used for backend automation, not entity recognition. Bot Service compliance relates to regulatory requirements, not geographic entity extraction.',
  },
  {
    id: 1022,
    text: 'You develop and deploy a Power Apps solution.\n\nThe following changes must be made to the solution:\n- Delete a column of data.\n- Modify several views.\n- Add several charts to dashboards.\n\nYou need to re-deploy the app.\n\nWhat should you do?',
    type: 'single',
    choices: [
      'Update the solution.',
      'Upgrade the solution.',
      'Create a new solution.',
      'Patch the solution.',
    ],
    correct: [0],
    explanation: 'Updating the solution is correct for these types of changes (deleting columns, modifying views, adding charts). Update maintains the solution version and applies incremental changes. Upgrade is used when moving from a lower major version to a higher one with breaking changes. Creating a new solution would lose solution history and customizations. Patching is for small fixes shipped between major releases and cannot delete components.',
  },
  {
    id: 1029,
    text: 'A company designs data integration with an external system by using virtual tables.\n\nYou need to implement the virtual tables.\n\nSolution: Implement an OData v4 provider as the data source.\n\nDoes the solution meet the goal?',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [0],
    explanation: 'Yes, OData v4 provider is a valid data source for implementing virtual tables in Dataverse. Virtual tables support OData v4 web services as external data sources, allowing you to query external data as if it were native Dataverse tables. This is one of the built-in virtual table data providers alongside SQL Server and Azure Cosmos DB.',
  },
  {
    id: 1030,
    text: 'A company designs data integration with an external system by using virtual tables.\n\nYou need to implement the virtual tables.\n\nSolution: Assign record ownership to individual users.\n\nDoes the solution meet the goal?',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [1],
    explanation: 'No, assigning record ownership to individual users is not a requirement for implementing virtual tables. Virtual tables are organization-owned by default and do not support user ownership. The ownership model for virtual tables is determined by the data provider configuration, not by individual user assignment.',
  },
  {
    id: 1031,
    text: 'A company designs data integration with an external system by using virtual tables.\n\nYou need to implement the virtual tables.\n\nSolution: Use a table that has a GUID as its primary key.\n\nDoes the solution meet the goal?',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [0],
    explanation: 'Yes, using a table with a GUID as its primary key is essential for implementing virtual tables. Dataverse requires all tables, including virtual tables, to have a GUID-based primary key to ensure unique record identification and compatibility with the Dataverse data model.',
  },
  {
    id: 1033,
    text: 'A company designs data integration with an external system by using virtual tables.\n\nYou need to implement the virtual tables.\n\nSolution: Create a calculated column on the virtual table.\n\nDoes the solution meet the goal?',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [1],
    explanation: 'No, calculated columns are not supported on virtual tables. Virtual tables connect to external data sources and do not store data in Dataverse, so server-side calculations cannot be performed. Calculated and rollup columns require native Dataverse storage to function properly.',
  },
  {
    id: 1047,
    text: 'Note: This question is part of a series of questions that present the same scenario.\n\nA university implements Dynamics 365 Sales. Several departments use opportunity records to bid for funding for projects within their own departments. You configure the system to ensure that each department can only work on their own records.\n\nEmployees in multiple departments often need to work together on an opportunity. Employees report that they are not able to see opportunities from other departments.\n\nYou need to ensure that employees from more than one department can work on the same opportunities when necessary.\n\nSolution: Use position hierarchy security and define the two departments as positions.\n\nDoes the solution meet the goal?',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [1],
    explanation: 'No, position hierarchy security is not the correct solution. Position hierarchy provides read-only access based on managerial reporting structures, not cross-departmental collaboration. It does not allow granting edit permissions and is designed for top-down visibility, not peer collaboration between departments.',
  },
  {
    id: 1049,
    text: 'Note: This question is part of a series of questions that present the same scenario.\n\nA university implements Dynamics 365 Sales. Several departments use opportunity records to bid for funding for projects within their own departments. You configure the system to ensure that each department can only work on their own records.\n\nEmployees in multiple departments often need to work together on an opportunity. Employees report that they are not able to see opportunities from other departments.\n\nYou need to ensure that employees from more than one department can work on the same opportunities when necessary.\n\nSolution: Use access team templates and give access to members in the two departments.\n\nDoes the solution meet the goal?',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [0],
    explanation: 'Yes, access team templates are the correct solution. Access teams allow you to share specific records with team members from different departments, granting them appropriate permissions (read, write, append, etc.) on individual records. This provides flexibility for cross-departmental collaboration while maintaining departmental security boundaries.',
  },
  {
    id: 1069,
    text: 'You are developing a Power Platform solution for a medical practice. You create a custom table named Doctors to record details about the doctors who work at the medical practice.\n\nYou must be able to attach a PDF copy of a doctor\'s medical license to the row for each doctor.\n\nYou need to configure the table.\n\nWhat should you do?',
    type: 'single',
    choices: [
      'Create a Power Automate flow to add attachments.',
      'Navigate to Table options and enable attachments.',
      'Navigate to Column options and enable attachments.',
      'Create relationships between the Doctor table and the Notes table.',
    ],
    correct: [1],
    explanation: 'Navigate to Table options and enable attachments is correct. In Dataverse, attachments are enabled at the table level through Table settings, not at the column level. Creating a Power Automate flow is unnecessary overhead. Column options do not have attachment settings. Creating relationships to the Notes table is the legacy approach; the modern method is using the table settings to enable attachments which automatically enables the Notes functionality.',
  },
  {
    id: 1075,
    text: 'The communication department for a company plans to add a publicly accessible survey page to the company’s public website.\n\nYou must add the new survey page to the company’s public website and capture data from the page to a Microsoft Dataverse environment.\n\nExplicit user credentials must not be required to write survey data to Dataverse.\n\nYou need to implement authentication.\n\nWhich authentication mechanism should you implement?',
    type: 'single',
    choices: [
      'AD FS',
      'Azure AD Conditional Access',
      'Azure guest account',
      'Client secret',
    ],
    correct: [3],
    explanation: 'Client secret is correct for anonymous authentication to Dataverse. Using an app registration with a client secret allows server-to-server (S2S) authentication without requiring user credentials. AD FS requires user authentication. Azure AD Conditional Access is for enforcing policies, not authentication. Azure guest account still requires user credentials to authenticate.',
  },
  {
    id: 1081,
    text: 'You are mapping data from an enterprise resource planning (ERP) system to Microsoft Dataverse.\n\nYou must reference the Name and Email from the ERP system during mapping to ensure that records are unique.\n\nYou need to create an alternate key that references the Name and Email columns.\n\nHow should you create the key?',
    type: 'single',
    choices: [
      'Add a Power Apps command function.',
      'Use Power Fx.',
      'Add columns to the Account table in Dataverse.',
      'Create a key in the Account table in Dataverse.',
    ],
    correct: [3],
    explanation: 'Creating a key in the Account table in Dataverse is the correct approach. Alternate keys are created at the table level in Dataverse and specify which columns must be unique. Adding a Power Apps command function does not create alternate keys. Power Fx is for canvas app logic. Adding columns alone does not create a key; you must explicitly define the key using those columns.',
  },
  {
    id: 1086,
    text: 'You enable change tracking on the Account table in Microsoft Dataverse.\n\nYou plan to use the Organization Service to retrieve the delta data by using C#.\n\nYou need to determine which message to use.\n\nWhat should you use?',
    type: 'single',
    choices: [
      'RetrieveAttributeRequest',
      'odata.track-changes',
      'RetrieveEntityChangesRequest',
      'UpdateEntityRequest',
      'UpdateRequest',
    ],
    correct: [2],
    explanation: 'RetrieveEntityChangesRequest is the correct message for retrieving delta data using the Organization Service in C#. This message returns changes to entity records since the last retrieval token. RetrieveAttributeRequest retrieves attribute metadata, not data changes. odata.track-changes is for Web API, not Organization Service. UpdateEntityRequest and UpdateRequest are for modifying data, not retrieving changes.',
  },
  {
    id: 1096,
    text: 'Which permissions does a managed identity have on Microsoft Dataverse data?',
    type: 'single',
    choices: [
      'permissions assigned to the corresponding application user',
      'permissions assigned to the user triggering the Azure resource',
      'permissions equivalent to the environment admin role',
      'permissions equivalent to the system administrator role',
    ],
    correct: [0],
    explanation: 'A managed identity has permissions assigned to the corresponding application user. When you configure a managed identity for Azure resources to access Dataverse, you create an application user in Dataverse and assign security roles to that application user. The managed identity inherits those specific permissions, not the triggering user\'s permissions or admin roles.',
  },
  {
    id: 1124,
    text: 'A company uses a model-driven app to record details of laboratory tests.\n\nYou are asked to create a custom component that makes it easier to capture multiple values from lab test results on mobile devices.\n\nYou need to create the interface for the dataset in case the mobile devices lose connection to the network.\n\nWhich method should you use?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'saveData',
      'updateView',
      'init',
      'getClient',
    ],
    correct: [1],
    explanation: 'The updateView method is correct for handling the dataset interface when devices lose connection. updateView is called by the framework when data changes or needs to be refreshed, making it ideal for updating the UI with cached or offline data. saveData is not a standard PCF method. init is only called once during component initialization. getClient returns client context information, not dataset operations.',
  },
  {
    id: 1127,
    text: 'You are creating a model-driven app.\n\nUsers need to see only the entities in the app navigation that are relevant to their role and their method of accessing the app.\n\nYou need to restrict entities on the sub-areas in the SiteMap.\n\nWhich properties should you use?\n\nPROPERTIES:\n- Client\n- Offline Availability\n- Privileges\n- SKUs\n\nREQUIREMENTS:\n1. Ensure that the entity is visible only if the user can create records.\n2. Ensure that the entity is visible only if the user is accessing the app with a web browser.',
    type: 'multiple',
    choices: [
      'Privileges',
      'Client',
      'Offline Availability',
      'SKUs',
    ],
    correct: [0, 1],
    explanation: 'Privileges controls visibility based on user permissions (e.g., Create, Read, Write), making the entity visible only if the user can create records. Client controls visibility based on how users access the app (Web, Outlook, Mobile), so setting it to Web makes the entity visible only in web browsers. SKUs is for licensing-based visibility (Online vs On-premises). Offline Availability controls whether the entity is available when offline.',
  },
  {
    id: 1128,
    text: 'You fix a bug in the code of your application, which is currently on version 10.0.2.1.\n\nYou need to publish an updated version of the solution.\n\nWhich version identifier should you use?\n\nOPTIONS:',
    type: 'single',
    choices: [
      '10.0.3.1',
      '10.0.2.2',
      '10.1.0.2',
      '11.0.0.0',
    ],
    correct: [0],
    explanation: 'Version 10.0.3.1 is correct because solution versioning follows semantic versioning where the third number (build/patch) should be incremented for bug fixes. Going from 10.0.2.1 to 10.0.3.1 indicates a patch release. 10.0.2.2 would also be acceptable for minor fixes. 10.1.0.2 implies a minor feature release. 11.0.0.0 implies a major breaking change release.',
  },
  {
    id: 1131,
    text: 'An organization uses Dynamics 365 Sales. The organization has accounting and customer service departments.\n\nYou must restrict users in customer service from being able to change the value of the balance field on the Contact records. The accounting team must be the only team able to edit this field.\n\nYou need to create the appropriate solution without any customizations.\n\nWhat should you do first?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Enable field security for the balance field and grant the customer service team read and update permissions.',
      'Create a customer service form and role and make the balance field read-only.',
      'Enable field security for the balance field and grant the accounting team read and update permissions.',
      'Create an accounting form and role and make the balance field read-only.',
    ],
    correct: [2],
    explanation: 'Enabling field security and granting the accounting team read and update permissions is correct. Field-level security ensures only users in a field security profile can access the field. By granting only the accounting team update permissions, customer service users cannot edit the balance field by default. Granting customer service read and update would allow them to edit. Form-based solutions with read-only fields can be bypassed through bulk edit or API.',
  },
  {
    id: 1133,
    text: 'A travel company plans to track the address of places their clients visit in an entity named Destination.\n\nClient information is captured as contact records. Client records include links to the places that clients visit.\n\nThe company must be able to link multiple rating records to the new address record.\n\nYou need to expand the Rating entity to include contact, address, and rating information in one place.\n\nWhich three actions should you perform?\n\nOPTIONS:',
    type: 'multiple',
    choices: [
      'Create a 1:N relationship between the Contact system entity and the Address system entity named Destination.',
      'Create a mapping for the Contact–Rating relationship.',
      'Create a 1:N relationship between the Address system entity and the Rating entity.',
      'Create a 1:N relationship between the Contact system entity and the Rating entity.',
      'Create a mapping for the Destination–Rating relationship.',
      'Create a 1:N relationship between the Destination entity and the Rating entity.',
    ],
    correct: [3, 4, 5],
    explanation: 'The correct actions are: Create a 1:N relationship between Contact and Rating (linking clients to ratings), create a mapping for the Destination-Rating relationship (to copy field values when creating ratings from destinations), and create a 1:N relationship between Destination and Rating (linking addresses to ratings). The Contact-Address relationship is incorrect since Destination is the custom entity for addresses. The Address system entity should not be used directly.',
  },
  {
    id: 1134,
    text: 'A company uses Dynamics 365 Sales and the Microsoft Online Services portal.\n\nThe multi-select OptionSet field data type is not supported in the portal.\n\nYou need to copy the selected field value to a text field.\n\nHow should you configure the Organization service request?\n\nANSWER AREA:\nComplete these two parts:\n1. RetrieveAsIfPublished = ?\n2. attributeMetadata = ?\n\nAVAILABLE VALUES:\n- true\n- false\n- AttributeResponse\n- OptionMetadataCollection\n- MultiSelectPicklistAttributeMetadata\n- EnumAttributeMetadata',
    type: 'multiple',
    choices: [
      'RetrieveAsIfPublished = true',
      'attributeMetadata = MultiSelectPicklistAttributeMetadata',
      'RetrieveAsIfPublished = false',
      'attributeMetadata = EnumAttributeMetadata',
    ],
    correct: [0, 1],
    explanation: 'RetrieveAsIfPublished = true retrieves the latest metadata including unpublished changes, which is needed when working with recently modified attributes. MultiSelectPicklistAttributeMetadata is the correct metadata type for multi-select option sets, providing access to all selected options. EnumAttributeMetadata is for single-select choice fields. Setting RetrieveAsIfPublished to false only returns published metadata.',
  },
  {
    id: 1136,
    text: 'You are creating a model-driven app.\n\nUsers need to see only the entities in the app navigation that are relevant to their role and their method of accessing the app.\n\nYou need to restrict entities on the sub-areas in the SiteMap.\n\nWhich properties should you use?\n\nPROPERTIES:\n- Client\n- Offline Availability\n- Privileges\n- SKUs\n\nREQUIREMENTS:\n1. Ensure that the entity is visible only if the user can create records.\n2. Ensure that the entity is not visible if the user is using an on-premises deployment.\n3. Ensure that the entity is visible only if the user is accessing the app with a web browser.',
    type: 'multiple',
    choices: [
      'Privileges',
      'SKUs',
      'Client',
      'Offline Availability',
    ],
    correct: [0, 1, 2],
    explanation: 'Privileges controls visibility based on user permissions (Create, Read, Write) - requirement 1. SKUs controls visibility based on deployment type (Online vs On-premises) - requirement 2. Client controls visibility based on access method (Web, Outlook, Mobile) - requirement 3. Offline Availability is not needed as it controls offline access, not the visibility requirements mentioned.',
  },
  {
    id: 1138,
    text: 'You create a Power Apps app that integrates with Dynamics 365 Customer Service.\n\nYou update the app and run Solution checker on the original solution. You receive an error stating that Solution checker cannot export the solution.\n\nYou need to determine the primary cause for the issue.\n\nWhat is the primary cause?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'The original solution is locked because there is a dependent patch.',
      'The solution was not exported before running Solution checker.',
      'The environment is in Administrator mode.',
      'Solution checker cannot check default solutions.',
    ],
    correct: [0],
    explanation: 'The primary cause is that the solution is locked due to a dependent patch. When you create a patch for a solution, the original (base) solution becomes locked and cannot be exported or modified until the patches are deleted or cloned into a new solution version. Solution checker requires exporting the solution to analyze it. Administrator mode and exporting order are unrelated to this error.',
  },
  {
    id: 1140,
    text: 'You are developing a Power Platform solution. You plan to add three buttons to a form.\n\nThe buttons have the following requirements:\n- Button1: Add the current date and time to the form when the button is selected.\n- Button2: Apply conditional logic to change the form color based on the option selected in the Title field.\n- Button3: Ensure that the word Emergency is entered as "Emergency".\n\nYou need to complete each button\'s action.\n\nFor each button, choose the correct command.',
    type: 'multiple',
    choices: [
      'Button1 = Now() only',
      'Button2 = Switch() or If()',
      'Button3 = IsMatch(TextInput1.Text, "emergency", Contains & IgnoreCase)',
      'Button1 = Today() only',
    ],
    correct: [0, 1, 2],
    explanation: 'Button1 uses Now() to get the current date AND time - Today() only returns the date without time. Button2 uses Switch() or If() for conditional logic to evaluate the Title field value and apply different colors. Button3 uses IsMatch with Contains & IgnoreCase flags to check if "emergency" appears in the text regardless of case.',
  },
  {
    id: 1151,
    text: 'A canvas app loads multiple collections and is slow when users navigate between screens.\n\nYou need to improve performance.\n\nWhat should you do?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Replace all instances of the ClearCollect method with the Connect method.',
      'Replace the existing code segment with a version that uses `Concurrent(...)` to load the collections in parallel.',
      'Replace the existing code segment with a version that keeps the calls sequential but splits them into multiple `Concurrent(ClearCollect(...))` blocks.',
      'Move all ClearCollect calls to the OnVisible property of each screen.',
    ],
    correct: [1],
    explanation: 'Using Concurrent() to load collections in parallel significantly improves performance because multiple data operations execute simultaneously rather than waiting for each to complete sequentially. The Connect method does not exist as a replacement for ClearCollect. Splitting sequential calls into multiple Concurrent blocks still executes sequentially between blocks. Moving to OnVisible may worsen performance by loading data on every screen navigation.',
  },
  {
    id: 1155,
    text: 'You are developing a model-driven app for a company that sells sporting goods. You add a button to a page that displays information for a product.\n\nThe button must copy information for the selected product and create a record in the data source. You add the following code to the button. (Line numbers are included for reference only.)\n\n```powerfx\n01 ?\n02 Products,\n03 Defaults(Products),\n04 {\n05   Name: Self.Selected.Item.Name,\n06   Description: Self.Selected.Item.Description,\n07   Type: Self.Selected.Item.Type,\n08   Price: Self.Selected.Item.Price\n09 }\n10 )\n```\n\nYou need to complete the code.\n\nWhich function should you insert at line 01?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'UpdateContext(',
      'IsMatch(',
      'LoadData(',
      'Patch(',
      'SaveData(',
    ],
    correct: [3],
    explanation: 'Patch() is the correct function to create or update records in a data source. It takes three parameters: the data source (Products), default values (Defaults(Products)), and a record with field values to set. UpdateContext is for setting context variables, not creating records. IsMatch is for pattern matching. LoadData and SaveData are for offline data caching, not creating records in a data source.',
  },
  {
    id: 1164,
    text: 'An organization uses Dynamics 365 Sales. The organization has accounting and customer service departments.\n\nYou must restrict users in customer service from being able to change the value of the balance field on the Contact records. The accounting team must be the only team able to edit this field.\n\nYou need to create the appropriate solution without any customizations.\n\nWhat should you do?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Enable field security for the balance field and grant the customer service team read and update permissions.',
      'Create a customer service form and role and a business rule that enables the balance field.',
      'Enable field security for the balance field and grant the customer service team read permissions.',
      'Enable field security for the balance field and grant the accounting team read permissions.',
    ],
    correct: [2],
    explanation: 'Enabling field security and granting customer service only read permissions prevents them from editing the balance field. When field security is enabled, all users lose access by default, so you must explicitly grant permissions. Granting read and update would allow editing. Creating forms with read-only fields or business rules can be bypassed through API or bulk operations. The accounting team should be granted read and update permissions separately.',
  },
  {
    id: 1166,
    text: 'You manage a model-driven app.\n\nThe forms in the app do not work as expected. You must troubleshoot the form issues. Code unrelated to an event must continue to run as you troubleshoot.\n\nYou need to disable form events to isolate the causes.\n\nWhich URL parameters should you use?\n\nAVAILABLE URL PARAMETERS:\n- DisableFormHandlers\n- DisableFormLibraries\n- DisableWebResourceControls\n\nEVENTS:\n1. Form OnLoad\n2. Column OnChange',
    type: 'multiple',
    choices: [
      'Form OnLoad -> DisableFormHandlers',
      'Column OnChange -> DisableFormLibraries',
      'Form OnLoad -> DisableWebResourceControls',
      'Column OnChange -> DisableFormHandlers',
    ],
    correct: [0, 1],
    explanation: 'DisableFormHandlers disables form event handlers like OnLoad and OnSave events at the form level. DisableFormLibraries disables JavaScript libraries which handle column-level events like OnChange. DisableWebResourceControls is for web resource controls, not event handlers. Using these parameters allows you to isolate which scripts are causing form issues while keeping other code running.',
  },
  {
    id: 1167,
    text: 'An organization uses Dynamics 365 Sales. The organization has accounting and customer service departments.\n\nYou must restrict users in customer service from being able to change the value of the balance field on the Contact records. The accounting team must be the only team able to edit this field.\n\nYou need to create the appropriate solution without any customizations.\n\nWhat should you do first?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Enable field security for the balance field and grant the customer service team read permissions.',
      'Create a customer service form and role and make the balance field read-only.',
      'Create a customer service form and role and a business rule that enables the balance field.',
      'Create an accounting form and role and a business rule that enables the balance field.',
    ],
    correct: [0],
    explanation: 'The first step is to enable field security for the balance field and grant the customer service team read-only permissions. This prevents customer service from editing while maintaining their ability to view the field. Creating forms with read-only fields can be bypassed. Business rules cannot enforce field-level security across all access methods. The accounting team would then be granted read and update permissions in a separate step.',
  },
  {
    id: 1172,
    text: 'You are writing C# code to create an alternate key in a table for two columns: Order ID and Order Date.\n\nThe table has thousands of records.\n\nYou use the CreateEntityKey message to create the key. When you retrieve EntityKeyMetadata.EntityKeyIndexStatus, it returns a value of Failed.\n\nYou need to resolve the issue with the index creation.\n\nWhat should you do first?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Use the ReactivateEntityKey action.',
      'Publish customizations.',
      'Use the DeleteEntityKeyRequest message.',
      'Delete duplicate combination values in Order ID and Order Date.',
    ],
    correct: [0],
    explanation: 'ReactivateEntityKey is the correct action when the EntityKeyIndexStatus is Failed. This action attempts to rebuild the index. The failure typically occurs due to duplicate key values or system issues during index creation. Publishing customizations does not fix failed key indexes. Deleting the key and recreating it is more drastic than reactivating. Deleting duplicates would only be necessary if the reactivation continues to fail due to non-unique values.',
  },
  {
    id: 1174,
    text: 'You have two tables, account and contact, with a parent-child relationship. Records in the account table can be created in the UI or from an external system integrated with Dataverse.\n\nWhen a user creates a record in the account table, a default record in the contact table must be created automatically.\n\nYou need to implement a process to create the contact records.\n\nSolution: Create a plug-in that runs when the account record is created. Use the Organization service to create the contact record.\n\nDoes the solution meet the goal?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [1],
    explanation: 'No, a plug-in using the Organization service within the same transaction context does not meet the goal for this scenario. Plug-ins that use the Organization service to create related records can cause issues with transaction depth and may not be the most efficient approach. The scenario likely requires asynchronous processing or a different automation approach to handle the contact creation properly.',
  },
  {
    id: 1176,
    text: 'You have two tables, account and contact, with a parent-child relationship. Records in the account table can be created in the UI or from an external system integrated with Dataverse.\n\nWhen a user creates a record in the account table, a default record in the contact table must be created automatically.\n\nYou need to implement a process to create the contact records.\n\nSolution: Create a cloud flow that runs when the account record is created. Use the Dataverse connector to create the contact record.\n\nDoes the solution meet the goal?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [0],
    explanation: 'Yes, a cloud flow triggered when an account record is created using the Dataverse connector is the correct solution. Cloud flows run asynchronously and can easily create related contact records without the transaction complexity of plug-ins. The Dataverse connector provides a low-code approach to automate record creation that is maintainable and follows Power Platform best practices.',
  },
  {
    id: 1187,
    text: 'An organization implements Dynamics 365 Sales.\n\nYou want to trigger a business rule when the main form is saved.\n\nWhat should you do?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Write a business rule to trigger on a change of the ModifiedOn field.',
      'Set the scope of the business rule to one specific form where the business rule triggers.',
      'Set the scope of the business rule to All Forms.',
      'Set the scope of the business rule to Entity.',
    ],
    correct: [3],
    explanation: 'Setting the scope to Entity ensures the business rule runs on save, regardless of which form is used. Entity-scoped business rules run server-side and trigger during any save operation including API calls. Form-scoped rules (single form or All Forms) only run client-side during form interactions. Triggering on ModifiedOn change is unreliable as it updates after the record is saved.',
  },
  {
    id: 1188,
    text: 'A company implements Dynamics 365 Sales.\n\nAn email notification must be sent automatically to the sales manager when a business process completes.\n\nYou need to ensure that email is sent.\n\nWhat should you create on the process completed trigger?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'A workflow',
      'An action step',
      'A data step',
      'A Power Automate cloud flow',
    ],
    correct: [0],
    explanation: 'A workflow is the correct choice for sending email notifications when a business process completes. Workflows can be triggered on process stage completion or completion events. Action steps are for calling custom actions, not sending emails directly. Data steps are for updating records. While Power Automate cloud flows could also work, the classic workflow approach is the traditional method for BPF completion triggers.',
  },
  {
    id: 1190,
    text: 'A customer wants to design a complex business process flow that includes six custom entities and four stages for each entity. One of the stages will have 15 steps.\n\nYou need to explain the flaw in this design to the customer.\n\nWhat is the flaw in this design?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'The maximum number of custom entities has been exceeded.',
      'The maximum number of steps for a stage has been exceeded.',
      'The maximum number of stages for an entity has been exceeded.',
      'The minimum number of stages for an entity has not been met.',
      'The minimum number of steps for a stage has not been met.',
    ],
    correct: [0],
    explanation: 'The maximum number of custom entities (5) has been exceeded. A business process flow can span up to 5 custom entities maximum. The design proposes 6 custom entities which violates this limit. The maximum steps per stage is 30 (15 is within limit). The maximum stages per BPF is 30 (24 total stages across entities is within limit). There is no minimum requirement for stages or steps.',
  },
  {
    id: 1198,
    text: 'An organization implements Dynamics 365 Supply Chain Management.\n\nYou need to create a Microsoft Flow that runs daily.\n\nWhat are two possible ways to achieve this goal? Each correct answer presents a complete solution.\n\nOPTIONS:',
    type: 'multiple',
    choices: [
      'Create the flow and set the flow frequency to daily and the interval to 1.',
      'Create the flow and set the flow frequency to daily and the interval to 24.',
      'Create the flow and set the flow frequency to hourly and the interval to 1.',
      'Create the flow and set the flow frequency to hourly and the interval to 24.',
    ],
    correct: [0, 3],
    explanation: 'To run a flow daily, you can either use "daily frequency with interval 1" (runs every 1 day) or "hourly frequency with interval 24" (runs every 24 hours, which equals once daily). Setting daily frequency with interval 24 would run every 24 days, not daily. Hourly with interval 1 runs every hour (24 times per day), which is not daily execution.',
  },
  {
    id: 1201,
    text: 'You create a cloud flow to process a list of records by using a loop.\n\nYou need to determine when to initialize a variable that is used to process the records.\n\nOPTIONS:',
    type: 'single',
    choices: [
      'After the loop',
      'After the first use of the variable inside the loop',
      'Before the loop',
      'Before the first use of the variable inside the loop',
    ],
    correct: [2],
    explanation: 'Variables must be initialized before the loop because Power Automate requires the Initialize Variable action to be at the top level of the flow, not inside loops or conditions. Initializing inside the loop would reset the value on each iteration. The variable must exist before any reference to it, so initializing after the loop or after first use would cause errors.',
  },
  {
    id: 1202,
    text: 'You are configuring a Microsoft Power Virtual Agents chatbot to use the authenticate option for Microsoft Teams only.\n\nYou need to select the variables that return information about the logged-in user.\n\nOPTIONS:',
    type: 'multiple',
    choices: [
      'authToken',
      'UserDisplayName',
      'UserID',
      'isLoggedIn',
    ],
    correct: [1, 2],
    explanation: 'UserDisplayName and UserID are the system variables that return information about the logged-in user in Power Virtual Agents with Teams authentication. UserDisplayName returns the user\'s display name, and UserID returns the unique identifier. authToken is used for making authenticated API calls but doesn\'t return user information directly. isLoggedIn is a boolean that indicates authentication status but doesn\'t provide user details.',
  },
  {
    id: 1224,
    text: 'You are developing a model-driven app for a company.\n\nWhen you create a new Account record, you must automatically display a form to collect data that is needed to create a Contact record. The form must switch to the appropriate form layout based on the contact type.\n\nYou open the Contact form by using JavaScript. You pass the contact type information to the form by using the Xrm.Navigation.openForm function. An OnLoad event handler in the Contact form processes the data and shows only the appropriate sections of the form for the given contact type.\n\nYou need to configure the receiving form to accept the data parameter.\n\nSolution: In the form editor, add an event handler for the data parameter.\n\nDoes the solution meet the goal?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [1],
    explanation: 'No, adding an event handler for the data parameter does not meet the goal. Event handlers are for form events (OnLoad, OnSave, OnChange), not for receiving parameters. The correct approach is to configure the form to accept query string parameters, which can then be read in the OnLoad event handler using getQueryStringParameters().',
  },
  {
    id: 1225,
    text: 'You are developing a model-driven app for a company.\n\nWhen you create a new Account record, you must automatically display a form to collect data that is needed to create a Contact record. The form must switch to the appropriate form layout based on the contact type.\n\nYou open the Contact form by using JavaScript. You pass the contact type information to the form by using the Xrm.Navigation.openForm function. An OnLoad event handler in the Contact form processes the data and shows only the appropriate sections of the form for the given contact type.\n\nYou need to configure the receiving form to accept the data parameter.\n\nSolution: In the form editor, add a web resource that sets formContext.data attributes.\n\nDoes the solution meet the goal?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [1],
    explanation: 'No, adding a web resource that sets formContext.data attributes does not meet the goal. Web resources cannot receive data passed from openForm. The correct approach is to configure the form to accept query string parameters, which are passed via the openForm function and can be retrieved in the OnLoad event.',
  },
  {
    id: 1231,
    text: 'You are developing a model-driven app for a company.\n\nWhen you create a new Account record, you must automatically display a form to collect data that is needed to create a Contact record. The form must switch to the appropriate form layout based on the contact type.\n\nYou open the Contact form by using JavaScript. You pass the contact type information to the form by using the Xrm.Navigation.openForm function. An OnLoad event handler in the Contact form processes the data and shows only the appropriate sections of the form for the given contact type.\n\nYou need to configure the receiving form to accept the data parameter.\n\nSolution: In the form editor, add a query string parameter for the data parameter.\n\nDoes the solution meet the goal?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [0],
    explanation: 'Yes, adding a query string parameter for the data parameter is the correct solution. In the form editor, you configure which query string parameters the form accepts. When using Xrm.Navigation.openForm with data parameters, those parameters are passed as query strings. The OnLoad event handler can then retrieve these parameters using getQueryStringParameters() or similar methods.',
  },
  {
    id: 1241,
    text: 'You are creating a model-driven app. You create JavaScript code to display a message when a record is saved.\n\nYou need to configure the associated JavaScript web resource name when adding the event handler to the form.\n\nWhich field should you use?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Event Type',
      'Component',
      'Function',
      'Library',
    ],
    correct: [3],
    explanation: 'The Library field is used to specify the JavaScript web resource that contains the function. When adding an event handler, you must first select the Library (web resource), then specify the Function name to call from that library. Event Type defines when the handler triggers. Component refers to which form element. The Function field specifies which function in the library to execute, but first the Library must be selected.',
  },
  {
    id: 1254,
    text: 'You are configuring two command buttons on a form.\n\nThe form must display the buttons only if conditions meet a pre-defined criteria.\n\nYou need to create rules for the form.\n\nWhich rule types should you use?\n\nAVAILABLE VALUES:\nFor website link to the current form:\n- PageRule\n- ValueRule\n- EntityRule\n\nFor sum of two form fields:\n- ValueRule\n- CustomRule\n- SelectionCountRule',
    type: 'multiple',
    choices: [
      'Website link to the current form -> PageRule',
      'Sum of two form fields -> ValueRule',
      'Website link to the current form -> EntityRule',
      'Sum of two form fields -> CustomRule',
    ],
    correct: [0, 1],
    explanation: 'PageRule is correct for checking website link conditions based on the current page or URL context. ValueRule is correct for evaluating conditions based on field values, including comparing or calculating field values like a sum. EntityRule is for enabling/disabling based on entity type. CustomRule would require custom JavaScript logic and is overkill for simple value checks. SelectionCountRule is for grid selection counts.',
  },
  {
    id: 1256,
    text: 'You are developing a model-driven app by using JavaScript.\n\nYou need to configure the app to display a dialog box when a form is opened or when a grid on a form is sorted.\n\nWhat should you use?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Grid OnSave',
      'Grid OnRecordSelect',
      'Grid OnChange',
      'Subgrid OnLoad',
    ],
    correct: [3],
    explanation: 'Subgrid OnLoad is the correct event because it fires when the subgrid is loaded, refreshed, or sorted. This includes sorting operations which reload the subgrid data. Grid OnSave does not exist for subgrids. Grid OnRecordSelect fires when a row is selected, not on sort. Grid OnChange fires when data changes, not on sort operations. The Subgrid OnLoad event captures all data refresh scenarios including sorting.',
  },
  {
    id: 1266,
    text: 'You develop a model-driven app in which client-side scripts are used extensively.\n\nUsers report that forms load quickly but become unresponsive.\n\nYou need to investigate the form issues before optimizing the forms.\n\nWhat should you do first?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Use a Quick View form.',
      'Add the URL parameter ribbondebug=true.',
      'Remove unused forms from the app.',
      'Enable Offline mode.',
      'Disable form event handlers.',
    ],
    correct: [1],
    explanation: 'Adding ribbondebug=true to the URL enables ribbon debugging which helps investigate performance issues with command bar and client scripts. This shows script execution details and timing. Quick View forms would not help diagnose client script issues. Removing forms or enabling Offline mode are optimization steps, not investigation steps. Disabling event handlers would help but ribbondebug provides more detailed diagnostic information.',
  },
  {
    id: 1273,
    text: 'An organization has a custom Assignments entity that guides agent actions. Team leaders for each assignment group must be able to review any changes made to assignment data by their agents.\n\nYou have a JSON delta query segment.\n\nFor each statement, select Yes or No.\n\nSTATEMENTS:\n1. You can use the JSON segment to retrieve a list of changes to the assignment records referenced in the segment.\n2. You can use the delta link to query the assignment changes from the last 30 days.\n3. You can use the delta link with a $filter option to retrieve assignment changes from the last 30 days.\n4. Is the delta link token valid?',
    type: 'multiple',
    ordered: true,
    choices: [
      '1. Yes',
      '1. No',
      '2. Yes',
      '2. No',
      '3. Yes',
      '3. No',
      '4. Yes',
      '4. No',
    ],
    correct: [0, 3, 5, 6],
    explanation: 'For delta queries: (1) Yes - the JSON segment can retrieve changes to assignment records. (2) No - delta links track changes from the last sync point, not a specific time period like 30 days. (3) No - $filter cannot be combined with delta links to filter by date range. (4) Yes - delta link tokens remain valid as long as they have not expired (typically 30 days without use). Delta queries track incremental changes since the last delta token.',
  },
  {
    id: 1285,
    text: 'A company needs to illustrate the relationships of the entities in Dynamics 365 Sales.\n\nYou need to select the appropriate tool to show this graphic.\n\nWhich tool should you select?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Metadata diagram',
      'Sales insights',
      'Power Automate',
      'Security model',
    ],
    correct: [0],
    explanation: 'Metadata diagram is the correct tool to graphically illustrate entity relationships in Dynamics 365. It shows tables, columns, and relationships visually using tools like the XrmToolBox Metadata Document Generator or the Metadata Diagram tool. Sales insights provides AI-driven sales recommendations, not diagrams. Power Automate is for workflows. Security model shows security configuration, not data relationships.',
  },
  {
    id: 1290,
    text: 'A field must display multiple selections to the user, enabling the user to select a value.\n\nYou need to determine which column type can support the required scenarios.\n\nWhich column type should you use?\n\nOPTIONS:\nColumn types:\n- Global choice\n- Lookup\n- Global choice and Lookup\n\nScenarios:\n- Remove a selection from being available without modifying existing records.\n- Must be completely deployed by using a solution.\n- Same set of selections can be used on multiple tables.',
    type: 'multiple',
    choices: [
      'Remove a selection from being available without modifying existing records. -> Lookup',
      'Must be completely deployed by using a solution. -> Global choice',
      'Same set of selections can be used on multiple tables. -> Global choice and Lookup',
      'Remove a selection from being available without modifying existing records. -> Global choice',
    ],
    correct: [0, 1, 2],
    explanation: 'Lookup is correct for removing selections without modifying records because you can deactivate/delete lookup records without affecting existing references. Global choice is correct for solution deployment because choices are solution-aware components. Both Global choice and Lookup can be reused across multiple tables - global choices are shared by definition, and lookups reference the same table. Local choices cannot be shared across tables.',
  },
  {
    id: 1296,
    text: 'You are creating a plug-in for an app that helps government employees get a proof of vaccination card.\n\nYou must add the following information to a vaccination record before a proof of vaccination card is created:\n- Vaccination type\n- Date of vaccination\n- Name of person administering the vaccine\n\nYou need to register the plug-in.\n\nIn which stage should you register the plug-in?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'PreValidation',
      'PostOperation',
      'MainOperation',
      'PreOperation',
    ],
    correct: [3],
    explanation: 'PreOperation is the correct stage for adding required information to a record before it is saved to the database. PreOperation runs after validation but before the main database operation, allowing you to modify the entity data. PreValidation is too early for setting data. PostOperation is too late as the record is already saved. MainOperation is the core system operation and cannot be customized.',
  },
  {
    id: 1317,
    text: 'You are designing an integration that sends created records from a custom Dataverse table to an exposed REST API.\n\nWhen the REST API is unavailable, you must be able to retry if the response status code is 500.\n\nSolution: Register a webhook by using the REST API as the endpoint. Wait for the webhook to automatically retry when a failure occurs.\n\nDoes the solution meet the goal?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [1],
    explanation: 'No, webhooks in Dataverse do not automatically retry on HTTP 500 errors. Webhooks are fire-and-forget and do not have built-in retry logic for transient failures. For retry capability, you should use Azure Service Bus integration or Power Automate flows which have built-in retry policies and error handling capabilities.',
  },
  {
    id: 1323,
    text: 'A company updates client contact information periodically. The Contact entity has\nalternate keys defined.\n\nThe code performs an UpsertRequest using key attributes and also sets the last name\nvalue directly on the entity.\n\nFor each statement, select Yes if true. Otherwise, select No.\n\nSTATEMENTS:\n1. If the last name is an alternate key and there are no contacts with the last\n   name Smith, the stored value of the last name is Smith.\n2. If the client number is an alternate key and the client number exists, the\n   stored value of the last name is Smith.\n3. If the last name is an alternate key and there are people who have the last\n   name Smith, the stored value of the last name is Smith.\n4. If the client number is an alternate key and the client number does not exist,\n   the stored value of the last name is Smith.',
    type: 'multiple',
    ordered: true,
    choices: [
      '1. Yes',
      '1. No',
      '2. Yes',
      '2. No',
      '3. Yes',
      '3. No',
      '4. Yes',
      '4. No',
    ],
    correct: [1, 3, 5, 7],
    explanation: 'This is a complex Upsert scenario question. The answers depend on how the alternate key and Upsert behave. Statement 1: Yes - new record created with Smith. Statement 2: Yes - existing record updated to Smith. Statement 3: Yes - updates to Smith (though this could cause issues if multiple matches exist). Statement 4: Yes - new record created with Smith. The key point is UpsertRequest either creates or updates based on whether the key match is found.',
  },
  {
    id: 1334,
    text: 'A company plans to replicate a Dynamics 365 Sales database into an Azure SQL\nDatabase instance for reporting purposes. The Data Export Service solution has\nbeen installed.\n\nYou need to configure the Data Export Service.\n\nWhich three actions should you perform? Each correct answer presents part of the\nsolution.\n\nOPTIONS:',
    type: 'multiple',
    choices: [
      'Enable auditing for all entities that must be replicated to Azure SQL Database.',
      'Create an export profile that specifies all the entities that must be replicated.',
      'Set up server-based integration.',
      'Enable change tracking for all entities that must be replicated to Azure SQL Database.',
      'Create an Azure SQL Database service in the same tenant as the Dynamics 365 Sales environment.',
    ],
    correct: [1, 3, 4],
    explanation: 'The three required actions are: Create an export profile (specifies which entities to export), Enable change tracking (required for Data Export Service to detect changes), and Create Azure SQL Database in the same tenant (destination for replicated data). Auditing is for tracking user actions, not data replication. Server-based integration is for SharePoint integration, not Data Export Service.',
  },
  {
    id: 1336,
    text: 'You are creating a Power Automate flow.\n\nYou create an Azure Service Bus listener app that receives requests from a\nthird-party application.\n\nWhen the flow calls the message queue, it must delete the message as soon as it\nis read.\n\nYou need to ensure that the queue is cleared properly.\n\nWhich method or class should you use?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'ReceiveMode',
      'BrokeredMessage',
      'EventHubReceiver',
      'EventHubSender',
    ],
    correct: [0],
    explanation: 'ReceiveMode is the correct answer. Setting ReceiveMode to ReceiveAndDelete causes messages to be deleted immediately upon reading, ensuring the queue is cleared. The default PeekLock mode keeps messages until explicitly completed. BrokeredMessage is the message object itself, not the delete mechanism. EventHubReceiver and EventHubSender are for Event Hubs, not Service Bus queues.',
  },
  {
    id: 1337,
    text: 'As part of the month-end financial closing process, a company uses a batch job to\ncopy all orders into a staging database.\n\nThe staging database is used to calculate any outstanding amounts owed by\nclients, and must process all historical data.\n\nYou need to ensure that only the data affected during the month is included in\nthe integration process.\n\nWhat are two possible ways to achieve this goal? Each correct answer presents a\ncomplete solution.\n\nOPTIONS:',
    type: 'multiple',
    choices: [
      'Use change tracking on the orders and run the integration to retrieve new',
      'Create a system view with the orders that have the Modified On field in the',
      'Use change tracking on the order lines and run the integration every week and',
      'Create a system view with the order lines that have the Modified On field in',
    ],
    correct: [0, 1],
    explanation: 'Two valid approaches: Change tracking on orders retrieves only modified records since last sync. Creating a system view filtering by ModifiedOn allows querying records changed within a date range. Using order lines change tracking or view is incorrect because the requirement is about orders, not order lines. Change tracking is more efficient for integration scenarios as it tracks deltas automatically.',
  },
  {
    id: 1344,
    text: 'The communication department for a company plans to add a publicly accessible survey page to the company\'s public website.\n\nYou must add the new survey page to the company\'s public website and capture data from the page to a Microsoft Dataverse environment.\n\nExplicit user credentials must not be required to write survey data to Dataverse.\n\nYou need to implement authentication.\n\nWhich authentication mechanism should you implement?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Claims based',
      'Microsoft 365',
      'Azure guest account',
      'Client secret',
    ],
    correct: [3],
    explanation: 'Client secret is the correct authentication mechanism for anonymous/public access to Dataverse. Using an Azure AD app registration with a client secret enables server-to-server authentication without requiring user credentials. Claims-based and Microsoft 365 authentication require user sign-in. Azure guest accounts still require individual user authentication.',
  },
  {
    id: 1350,
    text: 'A company requires custom validation when users save form records that use a synchronous plug-in.\n\nIf validation fails, a message that explains how to resolve the issue must be displayed on the form to the user.\n\nYou need to implement the custom validation.\n\nSolution: Include the message in the output parameters of the plug-in.\n\nDoes the solution meet the goal?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [1],
    explanation: 'No, output parameters do not display messages to users. Output parameters are used to pass data back to the calling code but do not trigger UI notifications. To display a validation error message to users, you should throw an InvalidPluginExecutionException with your custom message, which will be shown on the form.',
  },
  {
    id: 1351,
    text: 'A company requires custom validation when users save form records that use a synchronous plug-in.\n\nIf validation fails, a message that explains how to resolve the issue must be displayed on the form to the user.\n\nYou need to implement the custom validation.\n\nSolution: Use the tracing service to log the message.\n\nDoes the solution meet the goal?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [1],
    explanation: 'No, the tracing service logs messages for debugging purposes only. Trace logs are written to the plug-in trace log and are not displayed to users on the form. To show validation error messages to users, throw an InvalidPluginExecutionException with the custom message.',
  },
  {
    id: 1352,
    text: 'A company requires custom validation when users save form records that use a synchronous plug-in.\n\nIf validation fails, a message that explains how to resolve the issue must be displayed on the form to the user.\n\nYou need to implement the custom validation.\n\nSolution: Throw a custom exception with the message.\n\nDoes the solution meet the goal?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [1],
    explanation: 'No, throwing a generic custom exception is not the correct approach. While it would stop the operation, generic exceptions may not display user-friendly messages. The correct solution is to throw an InvalidPluginExecutionException which is specifically designed to display custom error messages to users on the form in a user-friendly format.',
  },
  {
    id: 1353,
    text: 'You develop code that will perform an update to existing records in a table.\n\nThe update must occur based on the alternate key configured for the table.\n\nYou need to perform the update.\n\nWhich two requests should you use? Each correct answer presents a complete solution.\n\nOPTIONS:',
    type: 'multiple',
    choices: [
      'UpdateRequest',
      'UpsertRequest',
      'CreateRequest',
      'RetrieveRequest',
    ],
    correct: [0, 1],
    explanation: 'Both UpdateRequest and UpsertRequest can use alternate keys to identify records. UpdateRequest with alternate key attributes updates existing records. UpsertRequest with alternate keys either updates existing records or creates new ones if not found. CreateRequest creates new records and does not use alternate keys for matching. RetrieveRequest is for reading data, not updating.',
  },
  {
    id: 1366,
    text: 'CONTEXT / QUESTION:\nA company has a new implementation that will use Dynamics 365 Field Service.\n\nA developer adds two custom tables to the solution in a development environment. The custom tables have lookup columns to Field Service tables.\n\nThe developer creates a new test environment and imports the solution. The developer receives an error that states dependencies are missing.\n\nWhat should you recommend?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Add the Field Service tables to the solution.',
      'Export the solution as unmanaged.',
      'Add the custom tables to the solution.',
      'Install the Field Service application in test.',
    ],
    correct: [0],
    explanation: 'Adding the Field Service tables to the solution resolves the missing dependency error. When custom tables have lookup columns to Field Service tables, those dependencies must be included in the solution for it to import successfully. Installing Field Service in test (option D) would also work, but adding tables to the solution is the recommended approach. Exporting as unmanaged does not solve dependency issues. The custom tables are already in the solution.',
  },
  {
    id: 1370,
    text: 'CONTEXT / QUESTION:\nA university implements Dynamics 365 Sales. Several departments use opportunity records to bid for funding for projects within their own departments. You configure the system to ensure that each department can only work on their own records.\n\nEmployees in multiple departments often need to work together on an opportunity. Employees report that they are not able to see opportunities from other departments.\n\nSolution: Share the individual opportunity that members of one department are working on with all members of the second department, and give those members the appropriate permissions.\n\nDoes the solution meet the goal?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Yes',
      'No',
    ],
    correct: [0],
    explanation: 'Yes, sharing individual opportunity records with specific permissions allows cross-department collaboration while maintaining security boundaries. Sharing grants explicit access to specific records for specific users or teams without changing the overall security model. This is the appropriate solution for occasional collaboration needs.',
  },
  {
    id: 1380,
    text: 'CONTEXT / QUESTION:\nYou are troubleshooting a new canvas app.\n\nUsers report the app loads slowly. You use the Monitor tool to view various events being performed in the app. Events performed in the app do not have formula details.\n\nYou need to enable formulas to be included with the Monitor tool events.\n\nWhat should you do?\n\nOPTIONS:',
    type: 'single',
    choices: [
      'Add the Microsoft Azure Application Insights data source to the canvas app.',
      'After each event, implement the trace function within the canvas app.',
      'Turn on the Debug published app setting in the canvas app.',
      'Validate the Application Insights instrumentation key has been populated in the app object\'s properties within the canvas app.',
    ],
    correct: [2],
    explanation: 'Turn on the Debug published app setting enables formula details to be captured in Monitor tool events. This setting must be enabled for the app to send detailed formula information to the Monitor. Application Insights is for telemetry collection, not Monitor tool integration. The trace function adds custom events but doesn\'t enable formula details. The instrumentation key is for Application Insights, not Monitor.',
  },
  {
    id: 1385,
    text: 'You need to modify the Power Automate flow to resolve CustomerC\'s issue.\n\nWhat should you do?',
    type: 'single',
    choices: [
      'Add a configure run that is set to is successful.',
      'Add a data operation that specifies the false conditions.',
      'Add a condition containing approval hierarchy.',
      'Add a timeout setting to the approval flow.',
    ],
    correct: [3],
    explanation: 'Adding a timeout setting resolves issues where approval flows hang indefinitely waiting for responses. Without a timeout, approvals can block the flow forever if the approver never responds. Configure run settings, data operations, and approval hierarchy do not address timeout-related issues.',
  },
  {
    id: 1388,
    text: 'You need to create the model-driven app for accounts designated as referrals.\n\nWhat should you add to the app?',
    type: 'single',
    choices: [
      'Workflow',
      'Subgrid',
      'Business rule',
      'Flow',
      'Chart',
    ],
    correct: [2],
    explanation: 'A business rule is the correct solution for designating accounts as referrals based on certain criteria. Business rules can set field values, show/hide fields, or validate data without code. Workflows and Flows are for automation, not form behavior. Subgrids display related records. Charts visualize data. Business rules are ideal for form-level logic like designating referrals.',
  },
  {
    id: 1389,
    text: 'You need to handle errors in UpdateRecord.js.\n\nWhich code segment should you add at line UR04?',
    type: 'single',
    choices: [
      'catch(error) { alert("Caught error: " + error.message); }',
      'Exception exception = Server.GetLastError(); if(exception != null)',
      'Catch(exception e){ console.writeline(e); }',
      'function (error) { console.log(error.message) }',
    ],
    correct: [3],
    explanation: 'The function(error) syntax is the correct callback function for handling errors in Web API calls using Promises or the Xrm.WebApi methods. Xrm.WebApi methods use .then(successCallback, errorCallback) pattern where the error callback receives the error object. The catch syntax is for try/catch blocks. Server.GetLastError() and console.writeline() are not valid JavaScript/Dataverse client API patterns.',
  },
  {
    id: 1394,
    text: 'You need to add the script for the registration form event handling.\n\nWhich code segment should you use?',
    type: 'single',
    choices: [
      'formContext.data.entity.addOnSave(myFunction)',
      'formContext.data.addOnLoad(myFunction)',
      'formContext.data.removeOnLoad(myFunction)',
      'addOnPreProcessStatusChange',
      'formContext.data.isValid()',
    ],
    correct: [0],
    explanation: 'formContext.data.entity.addOnSave(myFunction) registers a function to run when the form is saved. This is the correct method for handling registration form save events. addOnLoad is for form load events. removeOnLoad removes a load handler. addOnPreProcessStatusChange is for business process flow status changes. isValid() validates data but does not register event handlers.',
  },
  {
    id: 1395,
    text: 'You need to add the script to populate event data on the form.\n\nWhich code segment should you use?',
    type: 'single',
    choices: [
      'formContext.data.addOnLoad(myFunction)',
      'formContext.data.removeOnLoad(myFunction)',
      'formContext.data.entity.addOnSave(myFunction)',
      'addOnPreProcessStatusChange',
      'formContext.data.isValid()',
    ],
    correct: [0],
    explanation: 'formContext.data.addOnLoad(myFunction) registers a function to populate data when the form loads. This is the correct method for initializing form data on load. addOnSave is for save events. removeOnLoad removes handlers. addOnPreProcessStatusChange is for BPF status changes. isValid() checks data validity but does not register event handlers.',
  },
  {
    id: 1396,
    text: 'You need to configure that the mobile app meets the requirements for phone entries.\n\nWhich expression should you use?',
    type: 'single',
    choices: [
      'IsBlank',
      'IsType',
      'PlainText',
      'IsNumeric',
    ],
    correct: [3],
    explanation: 'IsNumeric is the correct expression to validate phone number entries contain only numeric characters. Phone numbers should consist of digits. IsBlank checks for empty values. IsType checks data types. PlainText converts rich text to plain text. IsNumeric ensures the input contains valid numeric characters for phone validation.',
  },
  {
    id: 1400,
    text: 'You need to create the customer mobile app.\n\nWhich type of function expression should you use?',
    type: 'single',
    choices: [
      'Filter',
      'Find',
      'LookUp',
      'Search',
    ],
    correct: [0],
    explanation: 'Filter is the correct function for returning multiple records that match criteria in a mobile app. Filter returns a table of records matching conditions. LookUp returns only the first matching record. Find is used for text searching within strings. Search is for full-text searching. For customer mobile apps that need to display lists of matching records, Filter is appropriate.',
  },
  {
    id: 1401,
    text: 'You need to identify the execution mode that is being used for the ISV solution reported by User5.\n\nWhich type of execution mode is in use?',
    type: 'single',
    choices: [
      'asynchronous',
      'atomicity',
      'transfer',
      'synchronous',
    ],
    correct: [3],
    explanation: 'Synchronous execution mode is correct when users experience blocking behavior while waiting for plug-in operations to complete. Synchronous plug-ins run in the same transaction and the user must wait for completion. Asynchronous runs in the background without blocking. Atomicity refers to transaction behavior. Transfer is not an execution mode for plug-ins.',
  },
  {
    id: 1402,
    text: 'You need to improve warehouse counting efficiency.\n\nWhat should you create?',
    type: 'single',
    choices: [
      'a flow that updates the warehouse counts as the worker performs the count',
      'a model-driven app that allows the user to key in inventory counts',
      'a Power BI dashboard that shows the inventory counting variances',
      'a canvas app that scans barcodes to allow a warehouse worker to select inventory counts',
    ],
    correct: [3],
    explanation: 'A canvas app with barcode scanning is the most efficient solution for warehouse counting. Scanning barcodes eliminates manual data entry errors and speeds up the counting process. A flow cannot scan barcodes. Model-driven apps are not optimized for mobile scanning scenarios. Power BI dashboards display data but do not support data entry or scanning.',
  },
  {
    id: 1414,
    text: 'You need to implement the background verification check stage field.\n\nWhich type of field should you use?',
    type: 'single',
    choices: [
      'Choice',
      'Status',
      'Choices',
      'Lookup',
    ],
    correct: [3],
    explanation: 'A Lookup field is correct for background verification check stages because it references records in a related table containing verification statuses or results. Lookups allow linking to detailed verification records. Choice fields are for simple static options. Status is a system field for record state. Choices (multi-select) would not be appropriate for a single verification stage status.',
  },
  {
    id: 1419,
    text: 'You need to determine the primary cause of the issue reported by interns when they use the app.\n\nWhat is the primary cause?',
    type: 'single',
    choices: [
      'Interns have the System Customizer security role but need the Environment Maker security role.',
      'Interns have the Common Data Service User security role but need the Environment Maker security role.',
      'Interns have the Environment Maker security role but need the Common Data Service User security role.',
      'Interns have the Environment Maker security role but need the System Customizer security role.',
      'Interns have the Environment Maker security role but need the Delegate security role.',
    ],
    correct: [2],
    explanation: 'Interns need the Common Data Service User (now called Basic User) security role to access data in the app. Environment Maker allows creating apps and flows but does not grant access to Dataverse data. Without the Basic User role, interns can make apps but cannot read or write data. System Customizer is for customization access. Delegate is for impersonation scenarios.',
  },
  {
    id: 1422,
    text: 'You need to reduce response time for the information email on the website.\n\nWhat should you create?',
    type: 'single',
    choices: [
      'a flow that creates a SharePoint item for each email response',
      'a flow that creates a notification in Microsoft Teams',
      'a Power Apps app that displays the number of email received in a dashboard',
      'a logic app that moves all emails received to Azure Blob storage',
    ],
    correct: [1],
    explanation: 'Creating a flow that sends notifications to Microsoft Teams reduces response time by immediately alerting staff when emails arrive. Teams notifications are instant and visible. SharePoint items require checking SharePoint. A Power Apps dashboard requires manual refresh. Azure Blob storage is for archival, not notifications. Real-time Teams notifications ensure quick response.',
  },
  {
    id: 1425,
    text: 'You need to configure the trigger for the priority field in the Account entity.\n\nWhich expression should you use?',
    type: 'single',
    choices: [
      'DIFFINWEEKS(now(),1)',
      'SUBTRACTDAYS(10, Now())',
      'ADDWEEKS(1, CreatedOn)',
      'DIFFINDAYS(CreatedOn, now())',
      'ADDDAYS(10, CreatedOn)',
    ],
    correct: [4],
    explanation: 'ADDDAYS(10, CreatedOn) adds 10 days to the creation date, which is useful for setting priority triggers based on record age. This calculates a date 10 days after creation. DIFFINWEEKS and DIFFINDAYS calculate differences between dates, not future dates. SUBTRACTDAYS is not a standard function. ADDWEEKS would add weeks, not days as typically needed for priority timing.',
  },
  {
    id: 1428,
    text: 'You need to improve the efficiency of counting warehouse inventory.\n\nWhat should you create?',
    type: 'single',
    choices: [
      'a model-driven app that allows the user to key in inventory counts',
      'a Power BI dashboard that shows the inventory counting variances',
      'a flow that updates the warehouse counts as the worker performs the count',
      'a canvas app that scans barcodes to allow a warehouse worker to select inventory counts',
    ],
    correct: [3],
    explanation: 'A canvas app with barcode scanning capabilities is the most efficient solution for warehouse inventory counting. Barcode scanning eliminates manual entry errors, speeds up the counting process, and works well on mobile devices used in warehouses. Model-driven apps are not optimized for mobile scanning. Power BI is for reporting. Flows automate processes but do not provide a scanning interface.',
  },
];